from django.apps import AppConfig

# This file is part of the Django project configuration for the 'chat' app.
class ChatsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'chats'
